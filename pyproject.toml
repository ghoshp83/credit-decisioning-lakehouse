# Tooling config for the Python helpers (ml/ feature+model pipeline, ai/ layer).
# This is not a packaged distribution -- just a home for ruff's settings so the
# lint/format rules are explicit and identical locally and in CI.
[tool.ruff]
target-version = "py311"
line-length = 88
